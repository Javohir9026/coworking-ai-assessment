# Domain types

Bu papka frontendning backend bilan typed kontraktini belgilaydi. Timestamp maydonlari UTC ISO 8601 string sifatida uzatiladi; pul qiymatlari binary floating-point o'rniga smallest-unit integer (`amountMinor`) sifatida ifodalanadi.
